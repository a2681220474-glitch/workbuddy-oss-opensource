# WorkBuddy OSS v1.0.9 Private Delivery Closeout

`v1.0.9` closes the current local-first private-use hardening batch.

## What Changed

- Synchronized package, API, docs, and memory state to `1.0.9`.
- Updated private-deployment expectations to the latest local version.
- Recorded the completed `v1.0.5` through `v1.0.9` route in release docs.
- Kept the remote ECS frozen at `v1.0.1` by user decision.

## Optimized

- The repo now has clearer continuation state for continuing after this batch.
- Local acceptance is the default gate before any future remote deployment.

## Fixed

- Fixed stale version text in the app shell and package metadata.

## Final Result

The local product line has session recovery, RBAC boundaries, safer config
copy, a local acceptance script, and a recognizable browser favicon.

## Manual Acceptance

1. Open `http://localhost:5173/` and confirm the `WB` favicon.
2. Confirm the app shell shows `v1.0.9`.
3. Run `npm run check:local-acceptance`.
4. Confirm the remote ECS remains untouched unless explicitly redeployed later.

## Verification

- `npm run build:web`
- `.venv/bin/python -m compileall apps/api scripts`
- `npm run check:local-acceptance`

## Next

The next major decision is whether to continue with deeper encrypted secret
storage locally or package this line for a controlled `v1.1.0` remote upgrade.
